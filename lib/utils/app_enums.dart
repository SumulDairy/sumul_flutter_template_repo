enum PoppinsWeight {
  thin100,
  extraLight200,
  light300,
  regular400,
  medium500,
  semiBold600,
  bold700,
  extraBold800,
  black900,
}

enum HttpMethod { get, post, put, patch, delete }

enum AppLanguageEnum { en, hi, gu }

enum MainCategoryEnum { milk, dairyProduct, bakery }

enum ShiftEnum { M, E, G, T, O }

enum UnitTypeEnum { pouch, crates }

enum MenuEnum { orderManagement, orderHistory, cashManagement, reports }

enum CashStatusEnum { pending, completed, cancel }

enum DistOrderTypeEnum { summary, customerwise, productwise }

enum CrateEnum { normalcrate, smallcrate }

enum MonthYearEnum { Y, M }

enum MonthEnum {
  april,
  may,
  june,
  july,
  august,
  september,
  october,
  november,
  december,
  january,
  february,
  march,
}
